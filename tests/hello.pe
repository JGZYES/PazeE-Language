int fib(int n) {
    if (n < 2) return n;
    return fib(n - 1) + fib(n - 2);
}

int main(void) {
    printf("Hello, ARM64!\n");
    int i;
    int sum = 0;
    for (i = 0; i < 10; i++) {
        sum += fib(i);
    }
    printf("fib(0..9) sum = %d\n", sum);
    return 0;
}
